pipeline {
    agent { 
        node {
            label 'shell-agent'
            }
      }
    stages {
        stage('Build') {
            steps {
                echo "Building.."
                '''
            }
       }
    }
}
